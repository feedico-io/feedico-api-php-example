name: Set repository metadata

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  metadata:
    runs-on: ubuntu-latest
    steps:
      - name: Configure repository About section
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api --method PATCH "repos/${{ github.repository }}" \
            -f description='PHP starter: fetch Feedico affiliate merchants and coupon codes via REST API. Docs: feedico.io/docs' \
            -f homepage='https://feedico.io'

          echo '{"names":["feedico","coupon-api","affiliate-api","merchants","coupons","rest-api","php","api-example","affiliate-marketing","publisher-api"]}' \
            | gh api --method PUT "repos/${{ github.repository }}/topics" --input -
